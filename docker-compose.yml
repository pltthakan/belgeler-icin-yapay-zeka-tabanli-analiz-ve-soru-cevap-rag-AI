services:
  postgres:
    image: pgvector/pgvector:0.8.2-pg16
    container_name: rag-postgres
    environment:
      POSTGRES_DB: ragdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d ragdb"]
      interval: 10s
      timeout: 5s
      retries: 5

  rabbitmq:
    image: rabbitmq:3.13-management
    container_name: rag-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: rag
      RABBITMQ_DEFAULT_PASS: rag
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7.2-alpine
    container_name: rag-redis
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis-insight:
    image: redis/redisinsight:latest
    container_name: rag-redis-insight
    ports:
      - "5540:5540"
    volumes:
      - redis_insight_data:/data
    depends_on:
      redis:
        condition: service_healthy

  ai-service:
    build: ./ai-service
    container_name: rag-ai-service
    environment:
      DATA_DIR: /app/data
      EMBEDDING_MODEL_NAME: sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
      QA_MODEL_NAME: deepset/xlm-roberta-base-squad2
      DISABLE_QA_MODEL: "false"
      # İsteğe bağlı Ollama entegrasyonu. Ayarlanmadığında extractive QA fallback kullanılır.
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-}
      OLLAMA_MODEL: ${OLLAMA_MODEL:-}
      OLLAMA_TIMEOUT_SECONDS: ${OLLAMA_TIMEOUT_SECONDS:-30}
      RAG_MIN_RETRIEVAL_SCORE: ${RAG_MIN_RETRIEVAL_SCORE:-0.10}
      RERANKER_ENABLED: ${RERANKER_ENABLED:-true}
      RERANKER_MODEL_NAME: ${RERANKER_MODEL_NAME:-cross-encoder/mmarco-mMiniLMv2-L12-H384-v1}
      RERANKER_CANDIDATE_COUNT: ${RERANKER_CANDIDATE_COUNT:-20}
      PGVECTOR_DSN: postgresql://postgres:postgres@postgres:5432/ragdb
      REDIS_URL: redis://redis:6379/0
      RAG_ANSWER_CACHE_TTL_SECONDS: ${RAG_ANSWER_CACHE_TTL_SECONDS:-3600}
      RAG_EMBEDDING_CACHE_TTL_SECONDS: ${RAG_EMBEDDING_CACHE_TTL_SECONDS:-604800}
      RAG_PROFILE_CACHE_TTL_SECONDS: ${RAG_PROFILE_CACHE_TTL_SECONDS:-21600}
    ports:
      - "5001:5000"
    volumes:
      - ai_data:/app/data
      - hf_cache:/root/.cache/huggingface
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5000/api/health')"]
      interval: 15s
      timeout: 10s
      retries: 10

  backend:
    build: ./backend
    container_name: rag-backend
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      ai-service:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/ragdb
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_PORT: 5672
      SPRING_RABBITMQ_USERNAME: rag
      SPRING_RABBITMQ_PASSWORD: rag
      DOCUMENT_PROCESSING_RETRY_DELAY_MS: ${DOCUMENT_PROCESSING_RETRY_DELAY_MS:-30000}
      JWT_SECRET: dev-secret-key-change-this-value-minimum-32-chars
      AI_SERVICE_BASE_URL: http://ai-service:5000
      APP_UPLOAD_DIR: /app/uploads
      APP_BOOTSTRAP_ADMIN_EMAIL: ${APP_BOOTSTRAP_ADMIN_EMAIL:-}
    ports:
      - "8080:8080"
    volumes:
      - backend_uploads:/app/uploads

  document-worker:
    build: ./document-worker
    container_name: rag-document-worker
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      ai-service:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/ragdb
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_PORT: 5672
      SPRING_RABBITMQ_USERNAME: rag
      SPRING_RABBITMQ_PASSWORD: rag
      AI_SERVICE_BASE_URL: http://ai-service:5000
      DOCUMENT_PROCESSING_RETRY_DELAY_MS: ${DOCUMENT_PROCESSING_RETRY_DELAY_MS:-30000}
      DOCUMENT_PROCESSING_MAX_RETRIES: ${DOCUMENT_PROCESSING_MAX_RETRIES:-3}
      DOCUMENT_WORKER_CONCURRENCY: 1
      DOCUMENT_WORKER_MAX_CONCURRENCY: 2
    volumes:
      - backend_uploads:/app/uploads

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE_URL: http://localhost:8080
    container_name: rag-frontend
    depends_on:
      - backend
    ports:
      - "3000:80"

volumes:
  postgres_data:
  rabbitmq_data:
  redis_data:
  redis_insight_data:
  ai_data:
  hf_cache:
  backend_uploads:
